import React from "react";
import { InputProps } from "react-admin";
import { ReactJsonViewProps } from "react-json-view";
declare type Props = {
    source: string;
    label?: string;
    helperText?: string;
    jsonString?: boolean;
    reactJsonOptions?: Omit<ReactJsonViewProps, "src">;
} & InputProps;
export declare const JsonInput: React.FC<Props>;
export {};
