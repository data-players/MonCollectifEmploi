import React from "react";
import { FieldProps } from "react-admin";
import { ReactJsonViewProps } from "react-json-view";
declare type Props = {
    source: string;
    record?: any;
    jsonString?: boolean;
    reactJsonOptions?: Omit<ReactJsonViewProps, "src">;
} & FieldProps;
export declare const JsonField: React.FC<Props>;
export {};
